<%= form_with(model: class_status, local: true) do |form| %>
  <% if class_status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(class_status.errors.count, "error") %> prohibited this class_status from being saved:</h2>

      <ul>
      <% class_status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status %>
  </div>

  <div class="field">
    <%= form.label :score %>
    <%= form.number_field :score %>
  </div>

  <div class="field">
    <%= form.label :student_id %>
    <%= form.text_field :student_id %>
  </div>

  <div class="field">
    <%= form.label :class_list_id %>
    <%= form.text_field :class_list_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
